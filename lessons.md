# 经验记录

## 2026-08-04：先确认“自动运行”的边界

- 本项目中的“自动运行”默认指：用户点击一次按钮，数据更新、扫描、舆情、AI 研判、收益回填、回测和因子体检按顺序自动完成。
- 除非用户明确提出，不得把“流程全自动”扩展成 Windows 开机自启、计划任务或系统服务。
- 涉及“自动化”时，设计前先区分业务流程自动化与操作系统运行自动化。

## 2026-08-04：跨文件补丁先核对每个文件的实际上下文

- 一个补丁同时修改多个文件前，必须分别确认目标行存在；任一上下文不匹配都会让整个补丁失败。

## 2026-08-05：上下文切换后先确认当前工具入口

- 会话续接后工具名称可能变化；运行命令前先以当前工具清单为准，避免沿用旧入口。

## 2026-08-10：测试不能依赖开发机环境变量

- 「没配凭据要报未配置」这类用例，必须在夹具里显式把凭据环境变量删掉（按配置里声明的 `api_key_env` 取，不要写死），不能指望开发机上没导出过。
- 否则两种后果同时出现：换台机器结果不一样（测的是环境而不是代码），以及本该离线的用例真的去打线上接口。
- 一开始的做法是「跑全量前手工清掉 `WORKBENCH_AI_API_KEY`」——那是把环境依赖记在人的脑子里，已改成夹具里根治。

## 2026-08-10：命令行入口的默认参数要真的跑一次

- `tools/train_ml.py` 的 `--strategy` 默认值指向不存在的 `default.yaml`，同一文件里上一条注释记着同类问题（`settings["storage"]` 键名写错）。两次都是「一直显式传参，默认分支从没被走过」。
- 改完命令行入口，至少把不带任何参数的那条跑一遍。

## 2026-08-11：更新环境变量不等于必须重启 Codex

- Windows 用户级环境变量更新后，已运行进程的环境快照不会自动变化，但这不代表必须重启 Codex。
- 后续命令可直接读取 User 级 `WORKBENCH_AI_API_KEY`，再注入本次 Python 子进程；不得把“最省事的重启”说成必要条件。

## 2026-08-17：少用硬门控阻断整条流程

- 只有继续执行会造成结果失真、污染数据或无法完成计算时，才允许整批停止。
- 单只股票、单个接口或单个页面的局部问题，应优先隔离、明确记录并继续处理其他有效对象。
- 数据质量标准不能静默降低；例如仍要求单只股票具备 150 根 K 线，但历史不足的股票应退出本次评分，而不是拖停全部候选。
